---
string:
  - type: string
  - type: string
    format: uri
  - type: string
    format: ipv4
  - type: string
    format: ipv6
  - type: string
    format: date-time
boolean:
  - type: boolean
'null':
  - type: 'null'
integer:
  - type: integer
  - type: integer
    minimum: 0
    maximum: 1
number:
  - type: number
object:
  - type: object
    properties:
      key:
        type: string
  # Without type set
  - properties:
      key:
        type: string
enum:
  # No type for enum
  - enum:
    - val1
    - val2
array:
  - type: array
    items:
      type: string